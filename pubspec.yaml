name: civic_issue_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- UI Icons ---
  cupertino_icons: ^1.0.2

  # --- Firebase (Backend) ---
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0

  # --- AI & Features ---
  google_generative_ai: ^0.4.0  # For Gemini
  image_picker: ^1.0.7          # For Camera/Gallery
  geolocator: ^10.1.0           # For Location (Planned)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true